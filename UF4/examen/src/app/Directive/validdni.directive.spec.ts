import { ValiddniDirective } from './validdni.directive';

describe('ValiddniDirective', () => {
  it('should create an instance', () => {
    const directive = new ValiddniDirective();
    expect(directive).toBeTruthy();
  });
});
